server:
  port: 8888
spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI}
          default-label: main
          search-paths:
            - proyecto-04
          username: ${GIT_USER}
          password: ${GIT_TOKEN}
          clone-on-start: true