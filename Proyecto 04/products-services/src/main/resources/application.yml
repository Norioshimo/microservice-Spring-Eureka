

spring:
  application:
    name: products-service
  config:
    import: "optional:configserver:http://localhost:8888" # Buscar en el servidor de configuración
  profiles:
    active: default


  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_sp_cloud
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
